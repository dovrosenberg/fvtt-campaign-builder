import { TestContext } from '../types';

export const runCampaignTests = (context: TestContext) => {
  test.describe.serial('Campaign tests', () => {
    // create a campaign in the first setting

    // switch to second setting and confirmm campaign not there

    // switch back and add a session

    // add a second session

    // change the session # on 1st session and make sure the order changes 

    // add a third session and check session number

  });
};
