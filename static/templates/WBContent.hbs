{{! Requires:l
	WBContentData: {
	  showHomePage: boolean;   // should we show the home page (vs. regular content)
		homePageData: HomePageData
	  HomePage: function that returns the template name
	}
}}
{{#if showHomePage}}
	{{> (HomePage) homePageData }}
{{/if}}
